version: "3.8"

services:
  qr-scanner-server:
    build: ./server
    ports:
      - "5000:5000"
    volumes:
      - qr_data:/app/data
    restart: unless-stopped
    environment:
      - PORT=5000
      - DB_PATH=/app/data/qr_scans.db

volumes:
  qr_data:
